{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run Current File",
      "type": "shell",
      "command": "javac -cp lib/core.jar -d bin src/${fileBasename} && java -cp bin:lib/core.jar ${fileBasenameNoExtension}",
      "windows": {
        "command": "javac -cp lib\\core.jar -d bin src\\${fileBasename} && java -cp bin;lib\\core.jar ${fileBasenameNoExtension}",
        "options": {
          "shell": {
            "executable": "cmd.exe",
            "args": ["/C"]
          }
        }
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "presentation": {
        "panel": "shared",
        "clear": true,
        "reveal": "always"
      }
    }
  ]
}
